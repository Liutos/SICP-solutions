;;; Because the answer returned finally is used as the second parameter in the 
;;; call of function cons when constructing the aimed list but the function 
;;; call was began in the head of the original list.